version: '3.8'

services:
  homeflix:
    build: .
    container_name: homeflix
    ports:
      - "5000:5000"
    restart: always
    volumes:
      - ./homeflix.db:/app/homeflix.db
      - ./templates:/app/templates
      - ./static:/app/static
      - ./css:/app/css
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
